# AI_LOOP_GUIDE.md — 루프 시스템 감독관 문서

> AI 에이전트는 **모든 작업 완료 선언 전**, 이 문서의 3-Gate 기준을 자가 검증하고
> 맨 아래 "증거 보고서" 양식으로 결과를 출력해야 한다.
> 하나라도 미통과면 완료 선언 금지 — 수정 후 다시 검증한다 (루프).

---

## Gate 1: 필수 통과 기준 (Static Analysis — PASS or FAIL)

기술적 결함이 없는 상태를 강제한다. **전부 PASS여야 Gate 2로 진행.**

| # | 항목 | 명령 | 기준 |
|---|---|---|---|
| 1-1 | 타입 체크 | `npx tsc --noEmit` | 에러 0개 |
| 1-2 | 정적 분석 | `npm run lint` | 에러 0개 |
| 1-3 | 프로덕션 빌드 | `npm run build` | 성공 (에러 없음) |
| 1-4 | any 금지 | `grep -rn ": any" src/` | 신규 any 0개 |

> 테스트 프레임워크(Vitest) 도입 시 `1-5: 단위 테스트 100% 통과` 활성화 예정

## Gate 2: 정량적 측정 기준 (Metrics — 숫자로 통제)

성능과 품질이 저하되지 않도록 숫자로 확인한다.

| # | 항목 | 측정 방법 | 임계치 |
|---|---|---|---|
| 2-1 | 번들 크기 | `npm run build` 후 `du -sk dist` | 직전 빌드 대비 +10% 이내 (신규 라이브러리 추가 시 사유 보고) |
| 2-2 | 신규 의존성 | `git diff package.json` | 새 dependency 추가 시 반드시 보고서에 명시 |
| 2-3 | 기능별 성능 | spec에 정의된 기준 (예: 조회 < 500ms) | spec 값 준수 |

## Gate 3: 정성적 평가 기준 (Alignment — 1~5점 자가 채점)

기능만 대충 구현하고 끝내는 것을 막는다.
**점수만 적는 것 금지 — 반드시 객관적 근거(Evidence)와 수정 액션(Action)을 함께 적는다.**
**4점 미만이면 Action을 실행하고 재채점한다.**

| # | 항목 | 채점 기준 |
|---|---|---|
| 3-1 | 4계층 컨벤션 일치도 | pages→components→hooks→apis 구조, Vite alias, 네이밍이 기존 페이지(Main, Friend, Statistics)와 일치하는가 |
| 3-2 | 사용자 흐름 자연스러움 | 로딩/에러/빈 상태 처리, 상태 변경 시 UI 즉각 갱신, 네트워크 에러 등 예외 상황의 UX가 부드러운가 |
| 3-3 | 상태 관리 적절성 | 전역 상태는 Zustand, 지역 상태는 hook — 경계가 올바른가 |

---

## 인간 개입 경계선 (Human-in-the-Loop)

아래에 해당하면 **작업을 멈추고** "여기서부터는 인간의 판단이 필요합니다"라고 보고 후 대기한다.
임의로 진행 금지.

1. **데이터 스키마 변경** — Firestore 컬렉션 구조 변경, 기존 데이터 마이그레이션 필요 시
2. **보안 및 권한** — `firestore.rules`, Firebase Auth 로직, 토스 본인인증 연동 수정 시
3. **결제/금전 로직** — 금액 계산 공식 변경 등 금전적 영향이 있는 수정 시
4. **기획 범위 초과** — 구현 중 spec(PRD) 범위를 넘는 대규모 리팩토링이 필요해질 때
5. **배포** — `npm run deploy`는 절대 자율 실행 금지

---

## 증거 보고서 양식 (작업 완료 시 필수 출력)

```
## 📋 증거 보고서: {작업명}

### Gate 1 — 필수 기준
- [PASS/FAIL] 타입 체크 (tsc --noEmit): 에러 N개
- [PASS/FAIL] 린트 (npm run lint): 에러 N개
- [PASS/FAIL] 빌드 (npm run build): 성공/실패
- [PASS/FAIL] any 타입: 신규 N개

### Gate 2 — 정량 기준
- 번들 크기: NNN KB (직전 대비 +N%)
- 신규 의존성: 없음 / {목록 + 사유}
- spec 성능 기준: [충족/미충족]

### Gate 3 — 정성 평가
- 컨벤션 일치도: N/5 (근거: ~~~) (Action: ~~~)
- 사용자 흐름: N/5 (근거: ~~~) (Action: ~~~)
- 상태 관리: N/5 (근거: ~~~) (Action: ~~~)

### 인간 개입 필요 여부
- [무위험 / 위험] (이유: ~~~)
```
